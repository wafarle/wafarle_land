# 🔐 شرح نظام التراخيص - كيف يعمل؟

## 📖 فهرس المحتويات
1. [ما هو نظام التراخيص؟](#ما-هو-نظام-التراخيص)
2. [كيف يعمل النظام؟](#كيف-يعمل-النظام)
3. [رحلة التاجر - من البداية للنهاية](#رحلة-التاجر)
4. [أنواع الاشتراكات](#أنواع-الاشتراكات)
5. [آلية التحقق من الترخيص](#آلية-التحقق)
6. [استخدام الترخيص في المتجر](#استخدام-الترخيص)
7. [أسئلة شائعة](#أسئلة-شائعة)

---

## 🎯 ما هو نظام التراخيص؟

### المفهوم الأساسي:
نظام التراخيص هو **مثل رخصة القيادة للمتجر الإلكتروني!** 🚗

```
بدون ترخيص ❌  = لا يمكن استخدام النظام
مع ترخيص ✅   = المتجر يعمل بكامل طاقته
```

### ببساطة:
- التاجر يشتري **ترخيص** (License)
- يحصل على **مفتاح فريد** (License Key)
- يستخدم المفتاح لتفعيل متجره
- النظام يتحقق من صحة الترخيص
- المتجر يعمل حسب نوع الاشتراك

---

## ⚙️ كيف يعمل النظام؟

### 🔄 دورة العمل الكاملة:

```
┌─────────────────────────────────────────────────────────┐
│                    1. المدير (أنت)                      │
│              ينشئ ترخيص جديد للتاجر                     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│             2. النظام يولد مفتاح فريد                   │
│         مثل: ABCD-1234-EFGH-5678                        │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│        3. التاجر يستلم المفتاح والمعلومات               │
│     - مفتاح الترخيص                                     │
│     - نوع الاشتراك (أساسي/احترافي/مؤسسي)               │
│     - تاريخ الانتهاء                                    │
│     - الميزات المتاحة                                   │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│         4. التاجر يدخل المفتاح في متجره                 │
│            في صفحة التفعيل/الإعدادات                    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│         5. النظام يتحقق من صحة المفتاح                  │
│     ✓ هل المفتاح موجود؟                                │
│     ✓ هل الترخيص نشط؟                                  │
│     ✓ هل النطاق صحيح؟                                  │
│     ✓ هل الترخيص لم يُنته؟                              │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│              6. المتجر يعمل! ✅                          │
│     - الميزات تُفعّل حسب نوع الاشتراك                   │
│     - التاجر يبدأ البيع                                  │
│     - النظام يراقب الاستخدام                            │
└─────────────────────────────────────────────────────────┘
```

---

## 🛒 رحلة التاجر - من البداية للنهاية

### 👤 من وجهة نظر التاجر:

#### **المرحلة 1: الشراء 💳**

1. **التاجر يزور موقعك**
   ```
   www.yoursite.com/pricing
   ```

2. **يختار الباقة المناسبة:**
   - 📦 أساسي: $99/شهر
   - 💼 احترافي: $299/شهر  
   - 🏢 مؤسسي: $999/شهر

3. **يدفع ويسجل بياناته:**
   - اسم المتجر
   - البريد الإلكتروني
   - رقم الهاتف
   - نطاق المتجر (example.com)

#### **المرحلة 2: استلام الترخيص 📧**

يصل للتاجر **بريد إلكتروني** يحتوي على:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 مرحباً في وفرلي!

شكراً لاشتراكك في باقة "احترافي"

📝 معلومات الترخيص الخاص بك:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔑 مفتاح الترخيص:
   ABCD-1234-EFGH-5678

🏪 اسم المتجر:
   متجر أحمد للإلكترونيات

🌐 النطاق المسجل:
   ahmed-store.com

📦 نوع الاشتراك:
   💼 احترافي

📅 تاريخ البدء:
   1 يناير 2024

📅 تاريخ الانتهاء:
   31 ديسمبر 2024

✨ الميزات المتاحة:
   ✓ حتى 1000 منتج
   ✓ طلبات غير محدودة
   ✓ تعدد اللغات
   ✓ تخصيص كامل
   ✓ دعم فني متقدم
   ✓ تقارير وتحليلات

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📖 خطوات التفعيل:

1. سجل دخول لمتجرك
2. اذهب إلى: الإعدادات → الترخيص
3. أدخل مفتاح الترخيص
4. اضغط "تفعيل"
5. ابدأ البيع! 🚀

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### **المرحلة 3: التفعيل 🔓**

**في لوحة تحكم المتجر:**

```typescript
// الخطوات التي يتبعها التاجر:

1. يسجل دخول: ahmed-store.com/admin

2. يذهب لصفحة الترخيص: /admin/license

3. يجد نموذج مثل هذا:

   ┌─────────────────────────────────────────┐
   │  🔐 تفعيل الترخيص                      │
   ├─────────────────────────────────────────┤
   │                                         │
   │  🔑 مفتاح الترخيص:                     │
   │  ┌─────────────────────────────────┐  │
   │  │ ABCD-1234-EFGH-5678             │  │
   │  └─────────────────────────────────┘  │
   │                                         │
   │  🌐 نطاق المتجر:                       │
   │  ┌─────────────────────────────────┐  │
   │  │ ahmed-store.com                 │  │
   │  └─────────────────────────────────┘  │
   │                                         │
   │  [ تفعيل الآن ]                        │
   │                                         │
   └─────────────────────────────────────────┘

4. يدخل المفتاح ويضغط "تفعيل"

5. النظام يرسل طلب للـ Backend:
   POST /api/license/verify
   {
     "licenseKey": "ABCD-1234-EFGH-5678",
     "domain": "ahmed-store.com"
   }
```

#### **المرحلة 4: التحقق ✅**

**ما يحدث في الخلفية:**

```javascript
// النظام يتحقق من:

async function verifyLicense(licenseKey, domain) {
  // 1. هل المفتاح موجود في قاعدة البيانات؟
  const license = await getLicenseByKey(licenseKey);
  if (!license) {
    return { success: false, error: "مفتاح غير صحيح" };
  }

  // 2. هل الترخيص نشط؟
  if (license.status !== 'active') {
    return { success: false, error: "الترخيص غير نشط" };
  }

  // 3. هل النطاق مطابق؟
  if (license.domain !== domain) {
    return { success: false, error: "النطاق غير مطابق" };
  }

  // 4. هل الترخيص ساري المفعول؟
  if (!license.isPermanent && license.expiryDate < new Date()) {
    return { success: false, error: "الترخيص منتهي" };
  }

  // 5. كل شيء تمام! ✅
  return {
    success: true,
    license: {
      type: license.type,
      features: license.features,
      expiryDate: license.expiryDate,
      maxProducts: license.maxProducts,
      maxOrders: license.maxOrders
    }
  };
}
```

#### **المرحلة 5: العمل 🚀**

**المتجر الآن مُفعّل والتاجر يمكنه:**

```
✅ إضافة منتجات (حسب الحد المسموح)
✅ استقبال طلبات (حسب الحد المسموح)
✅ استخدام جميع الميزات المتاحة
✅ تخصيص المتجر
✅ إدارة المخزون
✅ متابعة التقارير
```

---

## 📦 أنواع الاشتراكات

### مقارنة تفصيلية:

<table dir="rtl">
<tr>
<th>الميزة</th>
<th>📦 أساسي<br>$99/شهر</th>
<th>💼 احترافي<br>$299/شهر</th>
<th>🏢 مؤسسي<br>$999/شهر</th>
</tr>

<tr>
<td><b>عدد المنتجات</b></td>
<td>حتى 100</td>
<td>حتى 1000</td>
<td>غير محدود ♾️</td>
</tr>

<tr>
<td><b>عدد الطلبات شهرياً</b></td>
<td>حتى 500</td>
<td>غير محدود ♾️</td>
<td>غير محدود ♾️</td>
</tr>

<tr>
<td><b>تعدد اللغات</b></td>
<td>❌</td>
<td>✅ (2 لغات)</td>
<td>✅ (غير محدود)</td>
</tr>

<tr>
<td><b>التخصيص</b></td>
<td>محدود</td>
<td>كامل</td>
<td>كامل + متقدم</td>
</tr>

<tr>
<td><b>الدعم الفني</b></td>
<td>بريد إلكتروني</td>
<td>بريد + شات</td>
<td>VIP 24/7</td>
</tr>

<tr>
<td><b>التقارير</b></td>
<td>أساسية</td>
<td>متقدمة</td>
<td>شاملة + AI</td>
</tr>

<tr>
<td><b>نطاقات إضافية</b></td>
<td>❌</td>
<td>1 نطاق</td>
<td>غير محدود</td>
</tr>

<tr>
<td><b>التحديثات</b></td>
<td>6 أشهر</td>
<td>سنة</td>
<td>مدى الحياة</td>
</tr>

<tr>
<td><b>API Access</b></td>
<td>❌</td>
<td>محدود</td>
<td>كامل</td>
</tr>

<tr>
<td><b>White Label</b></td>
<td>❌</td>
<td>❌</td>
<td>✅</td>
</tr>
</table>

---

## 🔍 آلية التحقق من الترخيص

### متى يحدث التحقق؟

```
🔄 التحقق التلقائي يحدث عند:

1. تسجيل الدخول للوحة التحكم
2. إضافة منتج جديد (للتحقق من الحد الأقصى)
3. استلام طلب جديد (للتحقق من الحد الأقصى)
4. الوصول لميزات متقدمة
5. كل 24 ساعة (تحقق دوري)
```

### الكود المستخدم:

```typescript
// في متجر التاجر - يتم التحقق

import { verifyLicense } from '@/lib/license-management';

// عند تسجيل الدخول
async function checkLicense() {
  const license = localStorage.getItem('license_key');
  const domain = window.location.hostname;
  
  const result = await verifyLicense(license, domain);
  
  if (!result.valid) {
    // إعادة توجيه لصفحة التفعيل
    router.push('/admin/license/activate');
  } else {
    // حفظ معلومات الترخيص
    localStorage.setItem('license_info', JSON.stringify(result.license));
    // السماح بالدخول
    router.push('/admin/dashboard');
  }
}

// عند إضافة منتج
async function addProduct(productData) {
  const licenseInfo = JSON.parse(localStorage.getItem('license_info'));
  const currentProducts = await getProductsCount();
  
  // التحقق من الحد الأقصى
  if (licenseInfo.maxProducts > 0 && 
      currentProducts >= licenseInfo.maxProducts) {
    alert('وصلت للحد الأقصى من المنتجات! قم بالترقية لإضافة المزيد.');
    return false;
  }
  
  // إضافة المنتج
  await saveProduct(productData);
  return true;
}
```

---

## 💻 استخدام الترخيص في المتجر

### التكامل مع النظام:

#### **1. صفحة التفعيل**
```typescript
// src/app/admin/license/activate/page.tsx

'use client';

import { useState } from 'react';
import { verifyLicense } from '@/lib/license-management';

export default function ActivateLicense() {
  const [licenseKey, setLicenseKey] = useState('');
  const [loading, setLoading] = useState(false);
  
  const handleActivate = async () => {
    setLoading(true);
    
    const domain = window.location.hostname;
    const result = await verifyLicense(licenseKey, domain, '1.0.0');
    
    if (result.valid) {
      // حفظ المفتاح
      localStorage.setItem('license_key', licenseKey);
      localStorage.setItem('license_info', JSON.stringify(result.license));
      
      alert('تم التفعيل بنجاح! 🎉');
      router.push('/admin/dashboard');
    } else {
      alert('خطأ: ' + result.message);
    }
    
    setLoading(false);
  };
  
  return (
    <div className="activation-page">
      <h1>تفعيل الترخيص</h1>
      <input
        type="text"
        value={licenseKey}
        onChange={(e) => setLicenseKey(e.target.value)}
        placeholder="XXXX-XXXX-XXXX-XXXX"
      />
      <button onClick={handleActivate} disabled={loading}>
        {loading ? 'جاري التحقق...' : 'تفعيل'}
      </button>
    </div>
  );
}
```

#### **2. Middleware للحماية**
```typescript
// middleware.ts

import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export async function middleware(request: NextRequest) {
  // التحقق من الترخيص للصفحات المحمية
  const licenseKey = request.cookies.get('license_key')?.value;
  
  if (!licenseKey && request.nextUrl.pathname.startsWith('/admin')) {
    // إعادة توجيه لصفحة التفعيل
    return NextResponse.redirect(new URL('/admin/license/activate', request.url));
  }
  
  // التحقق من صحة الترخيص
  // يمكن عمل cache للنتيجة لتقليل الطلبات
  
  return NextResponse.next();
}
```

#### **3. عرض معلومات الترخيص**
```typescript
// في لوحة التحكم

function LicenseInfo() {
  const licenseInfo = JSON.parse(localStorage.getItem('license_info'));
  
  return (
    <div className="license-card">
      <h3>معلومات الترخيص</h3>
      
      <div className="info-row">
        <span>النوع:</span>
        <strong>{licenseInfo.type}</strong>
      </div>
      
      <div className="info-row">
        <span>الحالة:</span>
        <span className={`status ${licenseInfo.status}`}>
          {licenseInfo.status === 'active' ? '✅ نشط' : '❌ غير نشط'}
        </span>
      </div>
      
      <div className="info-row">
        <span>تاريخ الانتهاء:</span>
        <span>
          {licenseInfo.isPermanent 
            ? '♾️ دائم' 
            : new Date(licenseInfo.expiryDate).toLocaleDateString('ar')}
        </span>
      </div>
      
      <div className="limits">
        <h4>الحدود:</h4>
        <ul>
          <li>
            المنتجات: {currentProducts}/{licenseInfo.maxProducts || '∞'}
          </li>
          <li>
            الطلبات الشهرية: {currentOrders}/{licenseInfo.maxOrders || '∞'}
          </li>
        </ul>
      </div>
      
      <button onClick={renewLicense}>
        تجديد الاشتراك
      </button>
    </div>
  );
}
```

---

## ❓ أسئلة شائعة

### س1: ماذا يحدث إذا انتهى الترخيص؟

**ج:** عند انتهاء الترخيص:
1. ⚠️ **تحذير مسبق**: يتم إرسال تنبيهات قبل 30، 15، 7 أيام
2. 🔒 **بعد الانتهاء**:
   - المتجر يظل يعمل للعملاء ✅
   - لا يمكن إضافة منتجات جديدة ❌
   - لا يمكن تعديل الإعدادات ❌
   - تظهر رسالة تحذير في لوحة التحكم
3. 💳 **التجديد**: يمكن التجديد بسهولة من لوحة التحكم

### س2: هل يمكن استخدام نفس المفتاح لأكثر من نطاق؟

**ج:** يعتمد على نوع الاشتراك:
- 📦 **أساسي**: نطاق واحد فقط
- 💼 **احترافي**: نطاقين (رئيسي + 1 إضافي)
- 🏢 **مؤسسي**: نطاقات غير محدودة

### س3: ماذا لو أردت تغيير النطاق؟

**ج:** يمكنك:
1. تواصل مع الدعم الفني
2. طلب تحديث النطاق
3. سيتم تحديثه خلال 24 ساعة
4. أو من لوحة التراخيص (للمدير)

### س4: هل يتم التحقق من الترخيص دائماً؟

**ج:** نعم، ولكن بذكاء:
- ✅ **عند تسجيل الدخول**: دائماً
- ✅ **إضافة محتوى جديد**: عند الحاجة
- ✅ **تحقق دوري**: كل 24 ساعة
- 💾 **Cache**: النتائج تُحفظ مؤقتاً لتحسين الأداء

### س5: ماذا لو فقدت مفتاح الترخيص؟

**ج:** لا مشكلة!
1. سجل دخول لحسابك
2. اذهب لـ "حسابي" → "التراخيص"
3. شاهد جميع تراخيصك ومفاتيحها
4. أو تواصل مع الدعم الفني

### س6: هل يمكن الترقية من باقة لأخرى؟

**ج:** بالتأكيد! ✅
1. اذهب لـ "الترخيص" → "ترقية الباقة"
2. اختر الباقة الجديدة
3. ادفع الفرق فقط (Prorated)
4. التفعيل فوري!

---

## 🔐 الأمان والحماية

### كيف نحمي النظام من الاستخدام غير المصرح؟

```typescript
// 1. تشفير المفتاح في قاعدة البيانات
const encryptedKey = encrypt(licenseKey);

// 2. التحقق من Domain بدقة
const requestDomain = req.headers.host;
const allowedDomains = license.domains;
if (!allowedDomains.includes(requestDomain)) {
  return { valid: false };
}

// 3. تتبع الاستخدام
await logLicenseCheck({
  licenseKey,
  domain: requestDomain,
  ipAddress: req.ip,
  timestamp: new Date(),
  action: 'verification'
});

// 4. كشف المحاولات المشبوهة
if (failedAttempts > 5) {
  await suspendLicense(licenseKey);
  await notifyAdmin(licenseKey, 'suspicious_activity');
}
```

---

## 📊 لوحة التحكم (للمدير)

### ما تراه أنت كمدير:

```
🎯 لوحة التراخيص (/licenses/dashboard)

┌────────────────────────────────────────┐
│  📊 الإحصائيات                         │
├────────────────────────────────────────┤
│  إجمالي المتاجر:        250            │
│  التراخيص النشطة:       230 (92%)      │
│  التراخيص المنتهية:     15  (6%)       │
│  التراخيص التجريبية:    5   (2%)       │
│  الإيرادات الشهرية:     $74,750        │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│  🏪 متجر أحمد للإلكترونيات             │
├────────────────────────────────────────┤
│  🔑 المفتاح: ABCD-1234-EFGH-5678       │
│  📧 البريد: ahmed@example.com          │
│  🌐 النطاق: ahmed-store.com            │
│  💼 النوع: احترافي                     │
│  ✅ الحالة: نشط                        │
│  📅 الانتهاء: 31/12/2024               │
│                                         │
│  📊 الإحصائيات:                        │
│    • المنتجات: 156                     │
│    • الطلبات: 450                      │
│    • المبيعات: $45,230                 │
│                                         │
│  [👁️ عرض التفاصيل] [✏️ تعديل] [🗑️ حذف]│
└────────────────────────────────────────┘
```

---

## 🎓 خلاصة النظام

### للتاجر:
```
1️⃣ يشتري الاشتراك
2️⃣ يستلم المفتاح
3️⃣ يفعّل متجره
4️⃣ يبدأ البيع
5️⃣ يستمتع بالميزات
```

### للمدير (أنت):
```
1️⃣ ينشئ تراخيص جديدة
2️⃣ يراقب الاستخدام
3️⃣ يجدد/يلغي التراخيص
4️⃣ يتابع الإحصائيات
5️⃣ يحصل على الإيرادات
```

### للنظام:
```
1️⃣ يولد مفاتيح فريدة
2️⃣ يتحقق من الصلاحيات
3️⃣ يفرض الحدود
4️⃣ يسجل الاستخدام
5️⃣ يحمي من الاستغلال
```

---

## 🚀 البدء السريع

### للتاجر الجديد:

```bash
# الخطوة 1: استلم معلومات الترخيص
✉️ افتح البريد الإلكتروني

# الخطوة 2: سجل دخول لمتجرك
🌐 yourstore.com/admin

# الخطوة 3: افتح صفحة الترخيص
⚙️ الإعدادات → الترخيص

# الخطوة 4: أدخل المفتاح
🔑 الصق: ABCD-1234-EFGH-5678

# الخطوة 5: فعّل
✅ اضغط "تفعيل الآن"

# الخطوة 6: ابدأ!
🎉 متجرك جاهز للعمل!
```

---

## 📞 الدعم والمساعدة

### للتاجر:
- 📧 البريد: support@wafrly.com
- 💬 الشات المباشر: في لوحة التحكم
- 📱 واتساب: +966-XX-XXX-XXXX
- 📖 قاعدة المعرفة: docs.wafrly.com

### للمدير:
- 🎯 لوحة التراخيص: `/licenses/dashboard`
- 📊 تفاصيل المتجر: `/licenses/store/[id]`
- 📝 الأدلة: راجع ملفات `.md`

---

**✨ الآن فهمت نظام التراخيص بالكامل! ✨**

إذا كان لديك أي سؤال آخر، لا تتردد في السؤال! 🙋‍♂️

